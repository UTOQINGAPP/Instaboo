import 'package:flutter/material.dart';

class InstallerPage extends StatelessWidget {
  const InstallerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Installer'),
      ),
      body: Center(
        child: Text('Installer'),
      ),
    );
  }
}