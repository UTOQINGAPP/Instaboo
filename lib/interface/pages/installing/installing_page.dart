import 'package:flutter/material.dart';

class InstallingPage extends StatelessWidget {
  const InstallingPage({super.key});

  @override
  Widget build(BuildContext context) {
      return Scaffold(
        appBar: AppBar(
          title: Text('Installing'),
        ),
        body: Center(
          child: Text('Installing'),
        ),
      );
  }
}